/*
I haven't finished reading the code so I'm not sure if this gets implemented.
If this is not implemented, remove it.
If it is implemented, you do not need a unique file for a small amount of code like this.
*/
function Backdrop(props) {
    return <div className="backdrop" onClick={props.onClick} />;
  }
   
  export default Backdrop;
  
